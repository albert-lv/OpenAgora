# 2026-06-08 — Arena + veRL 项目推进记录

## 上午：PR 合并

合并了 agent-arena 仓库的 3 个 PR 和 quickstart patch：
- ✅ PR #2 `feat/proxy-logprobs` — proxy 注入 logprobs 捕获
- ✅ PR #1 `feat/sdk-trajectory-content` — SDK 扩展 get_trajectory 返回 response content
- ✅ PR #3 `feat/verl-rollout-provider` — 修了 `bsz = len(prompts)` → `prompt_ids.shape[0]` 后合并
- ✅ Quickstart patch push 到 main（Makefile、run.sh、arena-server flag、mock_llm.py）

合并顺序：#2 → #1 → #3 → quickstart patch

## 中午：CI 修复

Python CI 失败原因：arena-verl 的 rollout_provider.py 引入 verl/torch/transformers/tensordict 等 import，但 pyproject.toml 只有 arena-sdk 一个依赖。

修复：
- `python/arena-verl/pyproject.toml` 添加 `torch`, `transformers`, `tensordict`, `numpy` 到 core dependencies
- `verl` 添加为 git 依赖（`https://github.com/volcengine/verl.git`），通过 `[tool.uv.sources]` 配置
- `uv.lock` 同步更新（从 163 行扩到 3787 行）
- Go CI 保持绿色

## 下午：token-level rewards + mock demo

### 实现
- `rollout_provider.py`：在 `generate_sequences()` 返回的 DataProto 中添加 `token_level_rewards`
- 策略：episode reward 均匀分配到每个 valid response token，padding 位置给 0.0
- 守恒验证：sum(token_level_rewards) == episode_reward
- 形状：`[bsz, response_length]`，dtype float32

### 文件
- `examples/verl-integration/demo_train_step.py` — 自包含 mock demo，纯 CPU 秒级跑完
- `examples/verl-integration/demo_token_reward.py` — 最小 mock demo，不依赖 torch/verl（numpy 模拟）
- `examples/verl-integration/README.md` — 中文文档
- `examples/quickstart/mock_llm.py` — 补上 logprobs.content 字段

### Bug 修复
- `run_rollout.py`：`rollout_id` 是 dict，需要 `rollout_id["rollout_id"]` 访问
- `run.sh`：检测 `.venv` 路径，支持 uv 项目的 python 位置
- `rollout_provider.py`：`verl.workers.config` 在 verl 0.4.1 中不存在，改为从 `verl.workers.rollout.base` 导入 `BaseRollout`
- `rollout_provider.py`：`BaseRollout` 是 `object`（非 ABC），移除 `super().__init__()` 调用

## 环境：pyenv + Python 3.11

- 系统：macOS 15.5 x86_64，Python 3.13（无 PyTorch wheel）
- 安装 pyenv 2.7.1 + Python 3.11.11（编译 ~10 分钟）
- 创建 `.venv-311`，安装 torch 2.2.2 CPU、transformers、tensordict、ray
- numpy 降级到 1.26.4（兼容 torch 2.2.2）
- verl 0.4.1 从 git 安装（依赖 ray、wandb、accelerate 等）
- pkg_resources 缺失：setuptools 版本问题，回退到 68.2.2 解决

## 当前状态

- main 分支 CI 全绿
- quickstart 5 分钟走通（实际 4 秒）
- veRL 接口数据流验证通过（mock 环境）
- ArenaRolloutProvider 可成功 import 和运行（mock 数据）
- 真实 PPO 训练 demo 需要 GPU 环境，本环境无法跑（macOS x86_64 + Python 3.11 只能装 torch 2.2.2，transformers 警告 PyTorch < 2.4）

## 下一步
- 投票通过：A1（veRL 社区 PR）+ B1（token-level reward）+ C1（可运行 demo）
- Moss 负责 veRL 社区 PR 准备
- Jarvis 负责 token-level reward 实现 + demo（已完成）
