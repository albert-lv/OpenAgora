#!/usr/bin/env bash
set -euo pipefail

# Quickstart: run a single rollout through Arena

ARENA_ENDPOINT="${ARENA_ENDPOINT:-localhost:9090}"
TASK_FILE="${TASK_FILE:-task.json}"

echo "Creating rollout..."
# TODO: use arena-sdk CLI or grpcurl
echo "Rollout created (not yet implemented — see README for manual steps)"
