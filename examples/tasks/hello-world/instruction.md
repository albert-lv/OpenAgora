Write a Python function `hello()` in `/sandbox/solution.py` that returns the string `"hello, world!"`.

The verifier will run:

```python
import solution
assert solution.hello() == 'hello, world!'
```

Signal completion by writing `/sandbox/.arena/done`.
