Write a Python function `hello()` in `/sandbox/solution.py` that returns `"hello, world!"`.

The solution will be scored on:
1. Correctness — does `hello()` return the expected string?
2. Style — does the file pass a simple lint check?
