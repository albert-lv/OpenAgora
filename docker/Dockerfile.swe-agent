FROM python:3.12-slim

WORKDIR /sandbox

# Install system dependencies
RUN apt-get update && apt-get install -y \
    git \
    curl \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# Install mini-swe-agent from GitHub
RUN pip install --no-cache-dir \
    git+https://github.com/SWE-agent/mini-swe-agent.git

# Install additional utilities that SWE-agent might need
RUN pip install --no-cache-dir \
    pytest \
    tree-sitter \
    tree-sitter-python

# Copy Arena adapter script
COPY docker/swe-agent-entrypoint.py /usr/local/bin/openagora-swe-agent
RUN chmod +x /usr/local/bin/openagora-swe-agent

ENTRYPOINT ["/usr/local/bin/openagora-swe-agent"]
